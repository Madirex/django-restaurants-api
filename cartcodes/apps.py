from django.apps import AppConfig


class CartcodesConfig(AppConfig):
    """Configuración de los códigos de carrito."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'cartcodes'
